spring:
  datasource:
    username: root
    password: "08052001"
    url: jdbc:mysql://127.0.0.1:3306/knowledge_platform?createDatabaseIfNotExist=true&useSSL=false

  jpa:
    hibernate:
      use-new-id-generator-mappings: false  # For compatibility with older DB schemas
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: safakochat9@gmail.com
    password: klsc ndrw plxl genv  # App-specific password (from Gmail)
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  servlet:
    multipart:
      enabled: true
      location: uploads/
      max-file-size: 10MB
      max-request-size: 10MB

  data:
    web:
      pageable:
        one-indexed-parameters: true
        default-page-size: 20

# Custom app properties
app:
  jwtSecret: YOUR_SECRET_KEY_HERE  # Replace with a strong secret key
  jwtExpirationMs: 0VmKiU7D7h      # 24 hours
  frontendUrl: http://localhost:5173  # Your Vue dev server URL
  upload-dir: uploads/              # Where uploaded files are stored